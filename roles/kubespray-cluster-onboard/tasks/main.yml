---
# tasks file for kubespray-cluster-onboard
- name: Print the gateway for each host when defined
  ansible.builtin.debug:
    msg: System {{ inventory_hostname }} Ansible version {{ ansible_version.string }}

- import_tasks: remove_section.yml

- import_tasks: generate_groups.yml

- import_tasks: add_groups_to_inventory.yml
